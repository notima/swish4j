<?xml version="1.0"?>
<project name="Swish-client-library" default="install" basedir=".">
    
    <target name="install">
        <exec dir="${basedir}" executable="bash" failonerror="true">
            <arg line="-l -c 'mvn install -DskipTests'"/>
        </exec>
    </target>

</project>
